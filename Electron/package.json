{
  "name": "server-downloader",
  "version": "1.0.0",
  "description": "A client to download from a host (Peer to Perr)",
  "main" : "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "start": "npm run build && electron ./dist/main.js",
    "package" : "cd packaged && electron-packager ../ --overwrite --all"
  },
  "author": "sshcraCK",
  "license": "ISC",
  "devDependencies": {
    "electron": "^9.0.4",
    "tslint": "^6.1.2",
    "typescript": "^3.9.5"
  },
  "dependencies": {
    "@types/async": "^3.2.3",
    "@types/jquery": "^3.5.0",
    "async": "^3.2.0",
    "electron-reload": "^1.5.0",
    "html-entities": "^1.3.1",
    "jquery": "^3.5.1"
  }
}
